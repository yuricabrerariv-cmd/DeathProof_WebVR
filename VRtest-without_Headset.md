How to test:

- Load the page as normal — scroll-driven animated camera works as before
- Press V — camera switches to the VR camera positioned at Camera_Animated's current location
- Click once on the canvas to lock the pointer, then move the mouse to look around freely
- Press Escape to release the pointer lock, then press V again to return to the animated camera

The VR button still works for real headset sessions (enters proper WebXR mode with head tracking).